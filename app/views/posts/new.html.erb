<h1>New Post</h1>

<%= form_for :post, :url => posts_path do|f| %>

	<p>
		<%= f.label :title %>
		<%= f.text_field :title %>
	</p>

	<p>
		<%= f.label :content %>
		<%= f.text_area :content %>
	</p>

	<p>
		<%= f.submit "Submit" %>
	</p>

<% end %>

<%= link_to "Back", posts_path %>